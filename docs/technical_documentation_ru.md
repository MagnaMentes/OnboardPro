# OnboardPro Техническая Документация

## © 2025 magna_mentes. Все права защищены.

## Содержание

1. [Архитектура системы](#архитектура-системы)
2. [Среда разработки](#среда-разработки)
3. [Backend API](#backend-api)
4. [Frontend реализация](#frontend-реализация)
5. [Схема базы данных](#схема-базы-данных)
6. [Миграции базы данных](#миграции-базы-данных)
7. [Тестирование](#тестирование)
8. [Развертывание](#развертывание)
9. [Безопасность](#безопасность)
10. [Устранение неполадок](#устранение-неполадок)

## Архитектура системы

OnboardPro следует современной архитектуре веб-приложения:

- **Backend**: FastAPI (Python) с базой данных SQLite и SQLAlchemy ORM
- **Frontend**: React с Tailwind CSS и Heroicons
- **Инфраструктура**: Docker и docker-compose для контейнеризации
- **Аутентификация**: JWT-based система аутентификации
- **Внешние интеграции**: Telegram Bot, Google Calendar, Workable API
- **Миграции**: Alembic для управления версиями базы данных

## Среда разработки

### Предварительные требования

- Docker и Docker Compose
- Node.js (v18+) и npm
- Python 3.11+

### Инструкции по установке

1. Клонировать репозиторий
2. Создать файл `.env` с необходимыми переменными окружения
3. Запустить `docker-compose up -d` для старта контейнеров
4. Открыть приложение по адресу http://localhost:3000

### Переменные окружения

Необходимые переменные окружения:

- `DATABASE_URL`: Путь к базе данных SQLite (по умолчанию: sqlite:///onboardpro.db)
- `SECRET_KEY`: Секретный ключ JWT для аутентификации
- `TELEGRAM_BOT_TOKEN`: Токен Telegram Bot API
- `GOOGLE_CREDENTIALS_PATH`: Путь к файлу учетных данных Google API
- `WORKABLE_API_KEY`: Ключ API Workable для интеграции с рекрутингом

Переменные окружения frontend:

- `REACT_APP_API_URL`: URL backend API (по умолчанию: http://localhost:8000)

## Backend API

### Эндпоинты аутентификации

- `POST /auth/login`: Вход пользователя
- `POST /auth/refresh`: Обновление JWT токена
- `GET /users/me`: Получение информации о текущем пользователе

### Управление пользователями

- `GET /users`: Список всех пользователей (только для HR)
- `POST /users`: Создание нового пользователя (только для HR)
- `PUT /users/{id}`: Обновление пользователя (только для HR)
- `DELETE /users/{id}`: Удаление пользователя (только для HR)

### Управление задачами

- `GET /tasks`: Список задач
- `POST /tasks`: Создание задачи
- `PUT /tasks/{id}`: Обновление всех полей задачи (только для HR, добавлено 29.04.2025)
- `PUT /tasks/{id}/status`: Обновление статуса задачи
- `DELETE /tasks/{id}`: Удаление задачи

### Управление планами

- `GET /plans`: Список планов адаптации (доступно для HR и менеджеров)
- `POST /plans`: Создание нового плана (только для HR)
- `PUT /plans/{id}`: Обновление плана (только для HR)
- `DELETE /plans/{id}`: Удаление плана (только для HR)

### Обратная связь

- `GET /feedback`: Список обратной связи
- `POST /feedback`: Создание обратной связи
- `PUT /feedback/{id}`: Обновление обратной связи
- `DELETE /feedback/{id}`: Удаление обратной связи

## Frontend реализация

### Структура компонентов

- `App.js`: Главный компонент приложения с маршрутизацией (обновлено 30.04.2025):
  - Реализован механизм защищенных маршрутов с проверкой авторизации
  - Автоматическое перенаправление неавторизованных пользователей на страницу логина
  - Контроль доступа к страницам в зависимости от роли пользователя
  - Вложенная структура маршрутов для единообразного пользовательского интерфейса
- `components/`: Переиспользуемые UI компоненты
  - `Layout.js`: Шаблон для всех страниц приложения (обновлено 30.04.2025):
    - Упрощена логика авторизации, теперь фокусируется только на UI
    - Адаптивная навигация с фильтрацией пунктов меню по роли пользователя
  - `ProtectedRoute.js`: Компонент защищенного маршрута (добавлено 30.04.2025):
    - Проверка наличия и валидности токена авторизации
    - Перенаправление неавторизованных пользователей на страницу логина
    - Проверка соответствия роли пользователя для доступа к определенным страницам
    - Отображение индикатора загрузки во время проверки авторизации
- `pages/`: Компоненты страниц
  - `Login.jsx`: Страница входа в систему (обновлено 30.04.2025):
    - Автоматическая проверка наличия токена при загрузке
    - Перенаправление на соответствующий дашборд в зависимости от роли пользователя
  - `Dashboard.jsx`: Панель управления сотрудника (обновлено 30.04.2025):
    - Улучшенное отображение карточек задач с цветовым выделением приоритета
    - Подсветка заголовка карточки в зависимости от приоритета задачи (высокий - красный, средний - желтый, низкий - зеленый)
    - Улучшенная структура карточек для лучшего визуального восприятия
  - `CalendarView.jsx`: Календарь задач с drag-and-drop функциональностью (обновлено 29.04.2025):
    - Интерактивный календарь с отображением задач в виде событий
    - Функциональность drag-and-drop для перемещения задач между датами
    - Автоматическое обновление дедлайнов задач при перетаскивании
    - Визуальное отображение приоритета задач с помощью цветовой маркировки
    - Поддержка разных представлений (месяц, неделя, день)
    - Исправлена ошибка при обновлении даты задачи через drag-and-drop (29.04.2025):
      - Изменён формат отправляемых данных на сервер с `due_date` на `deadline`
      - Добавлены обязательные поля модели `TaskCreate` при обновлении задачи
      - Приведение формата даты к ISO для корректного сохранения на бэкенде
  - `Profiles.jsx`: Страница управления пользователями:
    - Отображение пользователей, сгруппированных по ролям
    - В заголовке карточки отображается ФИО вместо email (обновление от 28.04.2025)
    - В теле карточки отображаются только телефон, email и отдел (обновление от 28.04.2025)
    - Фильтрация пользователей по статусу (все/активные/заблокированные)
    - Поиск пользователей по email или отделу
    - Создание, редактирование, блокировка/разблокировка и удаление пользователей
    - Функционал сброса пароля с отображением временного пароля
    - Исправлена проблема отображения фотографий пользователей (обновление от 28.04.2025)
  - `ManagerDashboard.jsx`: Интерфейс менеджера с двумя основными разделами:
    - Управление задачами:
      - Создание и управление задачами через модальные формы
      - Полное редактирование задач для пользователей с ролью HR (добавлено 29.04.2025)
      - Модальное окно редактирования задач с возможностью изменения всех полей (добавлено 29.04.2025)
      - Расширенная фильтрация задач по статусу, приоритету, сотруднику и плану (добавлено 28.04.2025)
      - Поиск задач по названию и описанию (добавлено 28.04.2025)
      - Сортировка задач по названию, приоритету, сроку и статусу (добавлено 28.04.2025)
      - Визуальная индикация статуса и приоритета задач
      - Замена иконки CircleStackIcon на QueueListIcon для статуса "В очереди" для улучшения визуального восприятия (добавлено 01.05.2025)
      - Возможность обновления списка задач из базы данных (добавлено 28.04.2025)
      - Счетчик отображаемых задач с информацией о примененных фильтрах (добавлено 28.04.2025)
    - Управление планами адаптации:
      - Просмотр списка планов адаптации (доступно для HR и менеджеров)
      - Создание планов через модальные формы (только для HR, добавлено 28.04.2025)
      - Редактирование существующих планов (только для HR, добавлено 28.04.2025)
      - Удаление планов без связанных задач (только для HR, добавлено 28.04.2025)
      - Проверка наличия связанных задач перед удалением (добавлено 28.04.2025)
      - Возможность обновления списка планов из базы данных (добавлено 28.04.2025)
      - Визуальные уведомления об успешных операциях с планами (добавлено 28.04.2025)
      - Контроль прав доступа на основе роли пользователя (добавлено 28.04.2025)
  - `HRDashboard.jsx`: HR Dashboard с аналитикой и обновлением в реальном времени (обновлено 02.05.2025):
    - Интегрированная система отображения аналитических данных в реальном времени
    - Интерактивные графики с визуализацией прогресса задач
    - Продвинутый механизм "умного" объединения локальных и серверных данных
    - Интеграция с WebSocket для обновления показателей без перезагрузки страницы
    - Расширенная фильтрация данных аналитики по дате, отделу и другим параметрам
    - Усовершенствованный механизм обхода кэша для получения актуальных данных (01.05.2025)
    - Исправлена проблема с обнулением счетчика "Задачи в процессе" после перезагрузки страницы (02.05.2025)
    - Улучшена синхронизация фактического списка задач и аналитических данных (02.05.2025)
    - Удалено дублирующее табличное представление блока "Задачи в процессе" для более чистого интерфейса (02.05.2025)

#### Механизм управления кэшированием данных аналитики (добавлено 01.05.2025)

- **На стороне сервера**:

  - Система версионирования для отслеживания актуальности данных в кэше
  - Глобальная версия аналитики для обеспечения согласованности всех данных
  - Автоматическая инвалидация кэша при изменении данных задач
  - Поддержка параметра `refresh=true` для принудительного обновления данных
  - Дополнительные метаданные для каждого ответа с временной меткой и информацией о версии
  - Сокращенное время хранения данных в кэше (до 2-5 секунд) для быстрого обновления

- **На стороне клиента**:
  - Модифицированные запросы с уникальным timestamp для обхода кэша браузера
  - HTTP-заголовки для отключения кэширования: `Cache-Control`, `Pragma`, `Expires`
  - Увеличенная задержка (3 сек) перед запросом обновлений для гарантии получения актуальных данных
  - Метки метаданных для отслеживания источника изменений (локальные vs. серверные)
  - "Умное" объединение данных с приоритезацией наиболее актуальных значений
  - Специальные механизмы для первой загрузки страницы с принудительным обновлением

#### Обработка WebSocket событий (обновлено 01.05.2025)

- **События обновления задач**:

  - Локальное обновление счетчиков задач при изменении статуса
  - Немедленное обновление UI для лучшего пользовательского опыта
  - Запрос дополнительного обновления аналитики с сервера для синхронизации
  - Оптимизированный процесс обработки удаления задач с корректным обновлением счетчиков

- **События обновления аналитики**:

  - Добавление метаданных real_time_update для приоритизации обновлений в реальном времени
  - Корректная обработка конкурирующих обновлений с разными источниками
  - Визуальные индикаторы и уведомления о получении новых аналитических данных

- `context/`: React контекст провайдеры
- `hooks/`: Пользовательские React хуки
- `utils/`: Утилиты

### Централизованный API-клиент

- **Модуль `config/api.js`**: Создан 28.04.2025 для централизованного управления API запросами
  - Автоматическое определение базового URL API в зависимости от среды
  - Единый интерфейс для всех API запросов в приложении
  - Автоматическое добавление авторизационных заголовков
  - Обработка ошибок, включая случаи с недействительными токенами
  - Автоматическая сериализация и десериализация данных
  - Исправление проблем с URL для изображений и API запросов (добавлено 28.04.2025)
  - Добавлена функция `validateToken` для проверки валидности токена (обновление от 30.04.2025)

**Основные функции API-клиента:**

```javascript
// Получение базового URL API
export const getApiBaseUrl = () => {
  return window.location.hostname === 'localhost'
    ? 'http://localhost:8000'
    : (process.env.REACT_APP_API_URL || '');
};

// Выполнение унифицированных API запросов
export const apiRequest = async (endpoint, options = {}) => {
  const baseUrl = getApiBaseUrl();
  const url = `${baseUrl}${endpoint}`;

  // Автоматическое добавление токена авторизации
  const token = localStorage.getItem('token');
  if (token && !options.headers?.Authorization) {
    options.headers = {
      ...options.headers,
      Authorization: `Bearer ${token}`
    };
  }

  // Обработка ошибок и статусов ответа
  // ...
};

// Тематические API для различных сущностей
export const usersApi = { ... };
export const authApi = {
  login: (email, password) => { /* ... */ },
  validateToken: () => usersApi.getCurrentUser(), // добавлено 30.04.2025
};
export const tasksApi = { ... };
```

### UI/UX дизайн

- **Стандартизация дизайна** (обновление от 29.04.2025):
  - Унифицированная структура заголовков на всех страницах с тегами `h2`, классами `text-2xl font-bold text-blue-600`
  - Добавлены краткие текстовые описания под заголовками страниц для лучшего UX
  - Стандартизированы контейнеры всех страниц с классами `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6`
  - Реализованы адаптивные горизонтальные отступы для разных размеров экранов
  - Приведен к единообразию стиль всех страниц приложения
- **Модальные окна**:
  - Унифицированный дизайн для всех модальных окон без использования сторонних библиотек (обновление от 28.04.2025)
  - Реализация с использованием собственных компонентов React и состояния (обновление от 28.04.2025)
  - Единый стиль заголовков с тематическими цветами (синий для основных действий, красный для удаления)
  - Полупрозрачный overlay с классами `bg-black bg-opacity-30`
  - Стандартизированные стили полей формы и кнопок
  - Последовательные отступы и расположение элементов
- **Организация разделов**: Контент организован в логические разделы с четкими заголовками
- **Адаптивный дизайн**:
  - Десктоп (≥1280px): Полная навигация с иконками и текстом
  - Планшет (≥768px, <1280px): Навигация только с иконками
  - Мобильный (<768px): Навигация через бургер-меню
- **Визуальная иерархия**:
  - Четкие заголовки разделов
  - Единый стиль кнопок
  - Цветовая индикация статуса
  - Интуитивные таблицы
- **Интерфейс шапки**:
  - Убрана информация о пользователе возле логотипа (обновление от 28.04.2025)
  - Логотип слева, навигация справа
  - Адаптивное меню для разных устройств

### Аналитические графики и визуализация данных

- **Обновленный компонент `AnalyticsChart`** (обновление от 30.04.2025):
  - Современный дизайн графиков с улучшенной цветовой гаммой
  - Плавные анимации для отображения данных с настраиваемой длительностью
  - Улучшенное отображение подписей и заголовков с использованием более четких шрифтов
  - Оптимизированные стили для различных типов графиков: круговых, столбчатых, линейных
  - Улучшенные всплывающие подсказки при наведении на элементы графиков
  - Адаптивный дизайн для корректного отображения на разных устройствах

### Дизайн карточек задач

- **Улучшенное визуальное представление приоритета задач** (обновление от 30.04.2025):
  - Цветовая кодировка заголовков карточек в зависимости от приоритета задачи
  - Высокий приоритет: заголовок с красноватым фоном
  - Средний приоритет: заголовок с желтоватым фоном
  - Низкий приоритет: заголовок с зеленоватым фоном
  - Сохранение цветовых бейджей приоритета для дополнительной визуальной информации
  - Улучшенная структура карточек с четким разделением на заголовочную и информационную части
  - Добавлены тонкие визуальные границы для лучшего восприятия

### Управление состоянием

- React Context API для глобального состояния
- Локальное состояние компонентов для UI элементов:
  - Состояния видимости модальных окон
  - Состояния форм
  - Состояния загрузки и ошибок
- Управление состоянием форм через контролируемые компоненты
- Система уведомлений через ToastContainer для информирования пользователя (добавлено 28.04.2025)

### Управление ресурсами

- **Изображения и статические файлы**:
  - Исправлена проблема с отображением фотографий пользователей (28.04.2025)
  - Реализовано правильное формирование путей к изображениям через getApiBaseUrl()
  - Резервное отображение иконок при ошибках загрузки изображений

## Схема базы данных

### Таблица Users

- `id`: Первичный ключ
- `email`: Email пользователя (уникальный)
- `password_hash`: Хеш пароля
- `role`: Роль пользователя (HR, Manager, Employee)
- `department`: Отдел пользователя
- `first_name`: Имя пользователя
- `last_name`: Фамилия пользователя
- `middle_name`: Отчество пользователя
- `phone`: Телефон пользователя в формате +380 XX XXX XX XX (обновление от 28.04.2025)
- `disabled`: Флаг блокировки пользователя (true/false)
- `photo`: Ссылка на фотографию пользователя (обновление от 28.04.2025)
- `created_at`: Временная метка создания
- `updated_at`: Временная метка обновления

### Таблица Tasks

- `id`: Первичный ключ
- `title`: Название задачи
- `description`: Описание задачи
- `user_id`: ID назначенного пользователя (внешний ключ)
- `plan_id`: ID плана (внешний ключ)
- `priority`: Приоритет задачи (низкий, средний, высокий)
- `status`: Статус задачи
- `deadline`: Срок выполнения
- `created_at`: Временная метка создания
- `updated_at`: Временная метка обновления

### Таблица Plans

- `id`: Первичный ключ
- `title`: Название плана
- `description`: Описание плана
- `role`: Целевая роль (сотрудник, менеджер)
- `created_at`: Временная метка создания
- `updated_at`: Временная метка обновления

### Таблица Feedback

- `id`: Первичный ключ
- `content`: Содержание обратной связи
- `from_user`: ID пользователя-отправителя (внешний ключ)
- `to_user`: ID пользователя-получателя (внешний ключ)
- `created_at`: Временная метка создания
- `updated_at`: Временная метка обновления

## Миграции базы данных

### Использование Alembic

Для управления схемой базы данных используется Alembic:

- **Инициализация миграций**: Миграции уже инициализированы в проекте
- **Конфигурация**: Настройки находятся в файле `alembic.ini`
- **Создание миграций**: `alembic revision --autogenerate -m "описание изменений"`
- **Применение миграций**: `alembic upgrade head`
- **Откат миграций**: `alembic downgrade -1`
- **История миграций**: `alembic history`

### Структура миграций

- `backend/migrations/env.py`: Конфигурация окружения миграций
- `backend/migrations/versions/`: Директория с файлами миграций
- `backend/alembic.ini`: Основной конфигурационный файл Alembic

### Механизмы устойчивости баз данных

Для обеспечения стабильной работы с базой данных реализованы следующие механизмы:

- **Проверка структуры БД при запуске**: Скрипт `start.sh` проверяет наличие необходимых таблиц и полей
- **Прямое обновление БД**: Скрипт `direct_db_update.py` для аварийного обновления структуры БД при проблемах с миграциями
- **Многоуровневый подход обновления**:
  1. Стандартный процесс миграции через Alembic
  2. Прямое обновление БД в случае неудачи миграций
  3. Аварийное создание недостающих таблиц и полей средствами SQLite

### Валидация соответствия моделей и БД

Инструмент `validate_db_models.py` позволяет:

- Проверить, соответствует ли структура БД моделям SQLAlchemy
- Выявить недостающие колонки и таблицы
- Получить рекомендации по исправлению проблем

## Тестирование

### Модульное тестирование

- **Фреймворк**: pytest
- **Директория тестов**: `backend/tests/`
- **Конфигурация**: `backend/pytest.ini`
- **Запуск тестов**: `pytest tests/test_api.py -v`
- **Тестовая БД**: `backend/test.db` (автоматически создается при запуске тестов)

### Нагрузочное тестирование

- **Инструменты**: Python `locust` для создания нагрузки
- **Сценарии**: `backend/load_test.py` содержит сценарии нагрузочного тестирования
- **Запуск**: `./run_load_test.sh` запускает тесты в Docker-контейнере
- **Конфигурация**: `docker-compose.loadtest.yml` содержит настройки для контейнеров тестирования
- **Анализ результатов**: `backend/analyze_load_test.py` для анализа логов и создания отчетов
- **Документация**: Подробная инструкция в `load_testing_guide.md`

#### Метрики нагрузочного тестирования

- Время отклика (медиана, 95-й, 99-й процентили)
- Запросов в секунду (RPS)
- Количество успешных/неуспешных запросов
- Использование CPU и памяти
- Пропускная способность сети

## Развертывание

### Docker развертывание

1. Создать файл `.env` с необходимыми переменными окружения
2. Собрать Docker образы: `docker-compose build`
3. Запустить контейнеры: `docker-compose up -d`
4. Открыть приложение по адресу http://localhost:3000

### Конфигурация контейнеров

- Backend контейнер:
  - Эндпоинт проверки здоровья: `/health`
  - Политика автоматического перезапуска: `unless-stopped`
  - Монтирование томов для разработки
- Frontend контейнер:
  - Сервер разработки на порту 3000
  - Политика автоматического перезапуска: `unless-stopped`
  - Монтирование томов для разработки
  - Том node_modules для производительности

### Продакшн развертывание

1. Настроить продакшн сервер
2. Настроить переменные окружения
3. Собрать и развернуть Docker контейнеры
4. Настроить обратный прокси (рекомендуется Nginx)
5. Настроить SSL сертификаты

## Безопасность

### Аутентификация

- JWT токены с истечением срока действия
- Хеширование паролей с bcrypt
- Контроль доступа на основе ролей

### Маршрутизация и авторизация

- **Защита маршрутов на стороне клиента** (обновление от 30.04.2025):
  - Компонент `ProtectedRoute` для проверки авторизации перед отображением страницы
  - Проверка валидности токена при навигации между страницами
  - Автоматическое перенаправление на страницу логина при отсутствии или истечении токена
  - Контроль доступа к специфичным страницам на основе ролей пользователя
  - Унифицированная система проверки прав доступа для всех защищенных страниц

### Защита данных

- Валидация входных данных
- Защита от SQL-инъекций
- Защита от XSS
- Защита от CSRF

### Безопасность API

- Ограничение частоты запросов
- Валидация запросов
- Обработка ошибок
- Защита переменных окружения

## Устранение неполадок

### Распространенные проблемы

- **Переменные окружения**: Убедитесь, что все необходимые переменные установлены в `.env`
- **Подключение к базе данных**: Проверьте переменную окружения DATABASE_URL
- **Аутентификация**: Проверьте секретный ключ JWT
- **Сборка Frontend**: Проверьте npm зависимости
- **Проблемы с Docker**: Проверьте установку Docker и docker-compose

### Проблемы с авторизацией и доступом к страницам

- **Перенаправление на страницу логина при попытке доступа к защищенной странице**:
  1. Проверьте наличие токена в localStorage с помощью инструментов разработчика
  2. Убедитесь, что токен не истек и является валидным
  3. Проверьте соответствие роли пользователя требуемым правам доступа для страницы
  4. При необходимости выполните повторный вход в систему
  5. Если проблема повторяется, очистите localStorage и кэш браузера

### Проблемы с отображением графиков и визуализаций

- **Графики отображаются некорректно или не отображаются**:
  1. Проверьте наличие данных для отображения в консоли браузера
  2. Убедитесь, что компонент `AnalyticsChart` получает корректные пропсы
  3. Проверьте соответствие структуры данных требуемому формату для Chart.js
  4. При проблемах с производительностью уменьшите количество точек данных с помощью параметра `maxPoints`

### Проблемы с обновлением данных

- **Операции с базой данных не отражаются в интерфейсе**:
  1. Проверьте корректность API запросов в консоли браузера
  2. Используйте кнопки обновления данных из базы (добавлено 28.04.2025) для синхронизации
  3. Проверьте правильность обработки ответов API на фронтенде
  4. При необходимости перезагрузите страницу для принудительного обновления данных

### Проблемы с функциональностью drag-and-drop в календаре

- **Ошибка "Не удалось обновить дату задачи" при перетаскивании события** (исправлено 29.04.2025):

  1. Проверьте формат данных, отправляемых на сервер в функции `handleEventDrop` в компоненте CalendarView.jsx
  2. Убедитесь, что используется поле `deadline` вместо устаревшего `due_date`
  3. Проверьте наличие всех обязательных полей модели `TaskCreate` в запросе на обновление:
     - `plan_id`
     - `user_id`
     - `title`
     - `description`
     - `priority`
  4. Убедитесь, что дата отправляется в формате ISO: `event.start.toISOString()`
  5. Проверьте в консоли ответ сервера на запрос обновления задачи
  6. При необходимости перезапустите приложение: `docker-compose restart`

- **События не перетаскиваются или обновление даты происходит некорректно**:
  1. Проверьте наличие атрибута `editable: true` в настройках календаря
  2. Убедитесь, что компонент FullCalendar правильно настроен с активированными опциями:
     ```javascript
     editable={true}
     droppable={true}
     eventDrop={handleEventDrop}
     ```
  3. Проверьте консоль браузера на наличие ошибок JavaScript
  4. Проверьте права доступа текущего пользователя (функциональность может быть ограничена для некоторых ролей)
  5. Перезагрузите страницу для обновления состояния календаря

### Логи

- Логи backend: `docker-compose logs backend`
- Логи frontend: `docker-compose logs frontend`

### Поддержка

Для технической поддержки обращайтесь к команде разработки по адресу support@onboardpro.com
